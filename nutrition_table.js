/**
 * @param {string} label
 * @param {string} value
 */
export function renderTableRow(label, value) {
    console.log(label, value);
    return `<tr>
    <td>${label} here</td>
    <td>${value} here</td>
</tr>`
}

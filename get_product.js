/**
 * @param {number} a
 * @param {number} b
 */
const getProduct = (a, b) => {
    console.log({a, b});
    let product = a * b;
    console.log({product});
    return product;
}

// Sample usage - do not modify
getProduct(2, 3);
